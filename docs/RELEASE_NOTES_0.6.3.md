# Monkez PyQt6 0.6.3

- Project requirements now install Monkez PyQt6 directly from the GitHub
  `main` branch because the project package is not assumed to be published on
  PyPI.
- `setup.bat` now stops immediately when uv dependency installation fails and
  never reports a false successful environment setup.
