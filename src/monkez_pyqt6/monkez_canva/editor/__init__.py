"""Editor boundary reserved for the Qt adapter layer.

The public canvas facade remains backwards compatible while Qt-free document,
runtime and registry packages can now be imported without constructing widgets.
"""

